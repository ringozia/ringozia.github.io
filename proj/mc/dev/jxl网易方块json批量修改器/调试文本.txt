基本框架是format_version、minecraft:block，minecraft:block内由description、components组成。
description、components内是我工作中经常需要改动到的地方。可以看到这两个字典内由若干个键值组成。
由于字典内键值的个数，参数各异。我需要你帮我写一个批量json修改工具，基于上面这份模板来开发。必须使用py2.7语法+ttk实现可视化ui，写成一份完整可运行的.py文件。

实现功能：浏览、修改、添加

自动识别.py同目录下文件夹 blocks下的若干.json文件用于修改。

ui界面由三个纵向长条形区域组成。

可视化ui界面中用一个列表展示来所有文件的文件名.json（文件多时，支持垂直滚动）
点击某个文件名json弹窗预览该文件内容，弹窗支持关闭。

第二个区域为快捷选择区域，自动识别所有json中description、components的所有键（重复的直接覆盖，description和components的分开显示，篇幅过长时支持支持垂直滚动）为一些快捷按钮（按钮上有对应键名文本）。点击某个键按钮时高亮，并且第三个区域窗口展示（自动抽取第一个含有该键的json）这个键的内容（有可能是字符串、浮点数、数值......），此时这个窗口也是编辑器，玩家可在此窗口进行手动编辑，编辑好点击保存，立即同步到所有.json文件，同时刷新第二区域键列表。（有可能某个文件并不包含这个键，所以需要额外加上去）界面底部小字说明修改情况，成功修改n个json

添加功能：支持添加不存在的键。第二区域底部有个新增键区域，点击后弹窗首先让用户选择是description还是components，下一步填写键名，第三部编写内容。填写完毕点击保存时，立即检查是否符合json格式，如果符合立即同步到所有，不符合底部小字说明情况。

界面顶部写明项目名称“jxl网易方块json批量修改器”版本V1.0.0 20260111，右上角有个说明按钮点击弹窗介绍项目使用方法。

（注意，项目中所说的同步和修改是指对源文件json进行源路径修改）

////////////////
非常好！现在只需要小改动：

修改后的json在排版顺序上，希望description永远在components之上，并且每个键按照字母倒序排列z-a。

第三区域支持删除键（在保存按钮的那一行），在选择某个键时，点击删除，自动同步删除该键。

在第二区域选择键后，在底部小字显示选择的键的名称。（保留按钮高亮）

支持键名汉化，在项目中预留一个字典给我，内部配置一些键名对应的中文名（依旧description和components分开），方便我自行修改。ui界面第二区域每一个键按钮将同时显示对应的中文名（英文/中文各一行，如果有的话）

在新增键窗口中，现在除了自定义键之外，支持选择系统内置的键。代码中依旧预留一个字典，每个字典由类（description/components）、键名、中文名、内容（内容为py格式，应用时能被转换为json格式）。

在新增键窗口中，系统内置的键以列表按钮形式罗列，每一个键按钮将同时显示对应的中文名（英文/中文各一行，如果有的话）数量多时支持滚动。

每一个弹出的窗口最好生成在父窗口中心。


没有提及的部分不要做修改和删减！！！


//////////////////////////
第二窗口虽然有滚动条，但是初始状态窗口没能显示出这个滚动条（被第三区域挡住了）请修复。

第二窗口可否实现鼠标滚轮垂直滚动列表的功能？

键新增弹窗打开后应该默认选择components。选择系统内置键时，点击键按钮，右边有个窗口同步展示该键的内容。窗口底部新增 加入 按钮，点击以 确认加入该键。

现在键选择按钮内 文本改成左对齐

目前ui窗口大小合适，不能再大了。

没有提及的部分不要做修改和删减！！！

///////
规避情况：避免出现初始窗口过小导致部分控件显示不全的问题，应该从其他地方优化，目前ui窗口大小合适，不能再大了，内部控件需要调整尺寸。


///////////////////////

bug修复：在读取每个json时，如果json文件中包含注释，需要先把全部注释删除保存后再读取。

在新增键窗口系统内置键中，description和components的键是需要分开展示的。

成功新增键后，第二区域鼠标滚轮滚动功能报错了。

三个区域宽度不是等宽的，预留一个参数让我调整初始宽度。

没有提及的部分不要做修改和删减！！！

///////////////
每次成功新增键后，最底部的那一个新增键按钮仍然滚动不到，应该多预留一些距离，或是尝试其他优化。

点击某个键按钮时高亮该按钮（直接改按钮颜色），点别的按钮时刷新。

现在键选择按钮的 两行文本都改成居中对齐。

在新增键窗口系统内置键页面中，预览窗太宽了，只需要当前的一半。

说明弹窗的文本太小了，说明窗口适当调整大些。

PANE_WEIGHTS 改为 [1, 5, 5]

没有提及的部分不要做修改和删减！！！

////////////////////
当编辑器存在修改痕迹后，

/////////////////
每次成功新增键后，按钮确实被添加了（UI 刷新了），但 Canvas 的 scrollregion 没有同步更新，导致新增内容“在可视区域之外”，滚动条滚到底也看不到。请修复。

在新增键窗口系统内置键页面中，预览窗是不允许编辑的，请禁止。

没有提及的部分不要做修改和删减！！！

